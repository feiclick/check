using System;
using System.Diagnostics;
using Novacode;
using RestSharp;



Validate Date Day - Sunday, 22 January 2017	Fields with case comparison


namespace ASI.AB.ApiTests
{
    public class ScheduleHelper
    {
        public RestClient restClient;
        public RestRequest restRequest;
        public string downLoadPath = "";       
        public int count = 1;

        public string GetDataFromWord(String path, int lineNumber)
        {
            // Getting all data from word document
            String returnTxt = "";
            int num = 0;
            Debug.WriteLine("reading a word doc");
            using (DocX doc = DocX.Load(path))
            {
                for (int i = 0; i < doc.Tables.Count; i++)                
                { 

                    foreach (var item in doc.Tables [i].Rows[i].Cells)
                       
                    {
                        Debug.WriteLine("content -- " + i + item);
                        num++;
                        if (num == lineNumber) { return item; }
                    }
                }
            }

            return returnTxt;
        }
    }
}
